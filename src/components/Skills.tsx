
import { useScrollAnimation } from '@/utils/animations';

const Skills = () => {
  // This component is intentionally left empty as per user request
  // to remove Skills & Competencies and Philosophy sections
  return null;
};

export default Skills;
